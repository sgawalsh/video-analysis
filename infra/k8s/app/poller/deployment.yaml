apiVersion: apps/v1
kind: Deployment
metadata:
  name: poller
  namespace: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: poller
  template:
    metadata:
      labels:
        app: poller
    spec:
      containers:
        - name: poller
          image: sgawalsh/video-analysis-worker:latest
          env:
            - name: role
              value: poller
          envFrom:
            - configMapRef:
                name: postgres-config
            - configMapRef:
                name: poller-config
            - secretRef:
                name: postgres-secret
